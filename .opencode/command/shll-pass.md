---
description: Set up pass + GPG identity + git remote for the password store.
agent: pass-wizard
---

Invoke the `pass-wizard` agent. Mirrors `.claude/skills/pass-wizard/SKILL.md`. Idempotent: re-running checks state at each step and only does the missing work.

$ARGUMENTS
