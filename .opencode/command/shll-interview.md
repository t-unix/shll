---
description: Run (or re-run) the shll interview, writing answers to state/user.toml.
agent: interview
---

Invoke the `interview` agent. Walks `manifests/interview.schema.toml` top-to-bottom and writes incremental answers to `state/user.toml`. Mirrors `.claude/skills/interview/SKILL.md`.

$ARGUMENTS
